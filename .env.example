# Never put real secrets in source control.

NEXT_PUBLIC_SITE_NAME="Osool Altamaioz"
NEXT_PUBLIC_DEFAULT_LOCALE="ar"
# BUSINESS_CONFIRMATION_REQUIRED — set final production domain before deploy
NEXT_PUBLIC_SITE_URL="https://osoolaltamaioz.com"

# Analytics (optional — scripts load only when set)
# NEXT_PUBLIC_GA4_MEASUREMENT_ID=G-XXXXXXXX
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# Catalog / database (MySQL — production target, Hostinger)
# Add password locally in .env.local only — never commit credentials.
DATABASE_URL="mysql://USERNAME:PASSWORD@srv519.hstgr.io:3306/u847758257_osoolstore"

# Auth (required in production)
AUTH_SECRET="change-me-in-production"

# Pricing / checkout behavior
# NEXT_PUBLIC_SHOW_DEMO_PRICES=true   # Show import demo prices in UI only (never treated as official)
# DEVELOPMENT_CHECKOUT_MODE=true      # Enables development test payment + demo-price checkout
# VAT_RATE=0.15

# Payments (Phase 8 — not active yet)
# PAYMENT_...=
# TABBY_...=
# TAMARA_...=

# Shipping (Phase 8 — not active yet)
# SHIPPING_...=
